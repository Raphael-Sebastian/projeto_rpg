<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fichas de D&D</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="header">
        <div class="title-bar">
            <span class="title">NOME</span>
            <span class="title">CLASSE</span>
            <span class="dice-icon">&#x2684;</span>
            <span class="title">NÍVEL</span>
            <span class="title">RAÇA</span>
        </div>
    </div>
    <div class="container">
        <table>
            <tbody>
                <?php foreach ($fichas as $ficha): ?>
                    <tr>
                        <td><?= htmlspecialchars($ficha['nome']); ?></td>
                        <td><?= htmlspecialchars($ficha['classe']); ?></td>
                        <td><?= htmlspecialchars($ficha['nivel']); ?></td>
                        <td><?= htmlspecialchars($ficha['raca']); ?></td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
        <a href="nova_ficha.php" class="btn">Nova Ficha</a>
    </div>
</body>
</html>